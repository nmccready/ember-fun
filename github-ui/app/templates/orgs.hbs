<h2>My Fav Orgs</h2>


<ul>
    {{#each model as |org|}}
    <li>
        <span {{action "favoriteClick" org}}>[ Favorite ]</span>
        {{link-to org.id 'org' org.id}}
    </li>
    {{/each}}
</ul>

{{!-- Unable to get data- attributeBindings working --}}
{{social-info 
    name="Nick McCready"
    twitter="nmccready"
    github="nmccready"
    on-link-toggle="linkToggled"}}

{{outlet}}